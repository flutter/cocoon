name: dev_check
description: Script to check dev.

environment:
  # The pub client defaults to an <2.0.0 sdk constraint which we need to explicitly overwrite.
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  cocoon_service:
    path: ../app_dart

